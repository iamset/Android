open class Arithmetic {
    // from the description, it implies that the input
    // will be gotten from the command line.
    fun add(a:String, b:String):Int = a.toInt() + b.toInt();
}

class Adder: Arithmetic(){

}
